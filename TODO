-remove the little blank square on the display. but how??
-a decent Makefile (autoconf?)
-auto fork in background and add -kill option
-make it work when vncserver and vncviewer are both running on X
